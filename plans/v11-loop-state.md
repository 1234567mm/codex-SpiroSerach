# V11 Loop State

Purpose: persistent memory for V11 local loops. Update this file at the start and end of each V11 slice.

## Current Status

- Branch: `codex/v11-dependency-freeze`
- Upstream: `origin/main`
- V11 baseline document: `plans/v11-lightweight-productionization-and-repository-plan.md`
- Loop spec: `plans/v11-loop-spec.md`
- Current phase: V11 P0 dependency freeze
- Current selected slice: `v11-dependency-freeze`
- Current selected slice status: verified in worktree; ready for review and merge
- Human gate: required before merge, push, deleting worktrees, changing scoring policy, changing artifact contracts, or exposing non-read-only API/MCP behavior.

## Dependency State

V11 assumes V10 scoring/review/manifest closure has landed. The next step is a read-only dependency freeze: generate or inspect representative artifacts, verify manifest metadata, and record any blockers before repository/API/frontend implementation starts.

| Dependency | Required for V11 | Current V11 stance |
|---|---|---|
| V10 manifest schema and artifact metadata | Repository facade, API/MCP, frontend discovery | Landed in V10; dependency freeze must record exact fields and sample outputs |
| `scoring-view.json` manifest-discovered and schema-valid | Scoring runtime, scoring eligibility UI | Artifact and runtime landed; freeze must verify sample manifest metadata |
| Provider confidence excluded from scoring effects | Scoring and provider lineage safety | Verified for scoring-view/runtime; keep as dependency-freeze assertion |
| Blocking review and missing reference scale excluded from scoring view | Scoring eligibility and review worklist | Scoring-view policy and review closure landed; freeze must verify sample joins |
| `review-events.jsonl`, `review-summary.json`, `recompute-markers.jsonl` | Review closure and recomputation flow | Landed in V10; dependency freeze must verify schema refs, hashes, record counts, and join keys |
| Resolved review writes curation status | Review closure and recomputation flow | Fixture-first path landed; dependency freeze must verify recompute marker and canonical joins |
| Stable join keys in manifest | Future frontend visualization | Landed in V10; freeze must produce the V11 join-key matrix |
| V10 loop state records selected slice, tests, blockers | V11 morning triage | Available; V10 now points to `v11-dependency-freeze` |

## Dependency Freeze Result

Status: implemented in branch `codex/v11-dependency-freeze`; not merged yet.

Verified sample bundles:

- `.v11-samples/enrichment-review-closure`
  - Generated by `python -m spirosearch.cli enrich --candidates .\.v11-samples\inputs\enrichment-candidates.json --output-dir .\.v11-samples\enrichment-review-closure --source-registry data\source_registry.json --review-events .\.v11-samples\inputs\review-events-input.jsonl`
  - Covers scoring view, canonical evidence, review closure, recompute markers, provider cache, provider cache index, and agent trace.
- `.v11-samples/v4-round`
  - Generated by `python -m spirosearch.cli v4-round --candidates data\seed_candidates.json --output-dir .\.v11-samples\v4-round --batch-size 2 --budget 100`
  - Covers recommendations, ledger, posterior, model updates, and V4 agent trace.

Validation evidence:

- Targeted dependency baseline: `tests.test_run_artifacts tests.test_provider_schemas tests.test_enrichment_runtime_cli tests.test_artifact_viewer tests.test_v4_runtime_cli tests.test_scoring_view tests.test_review_runtime -v` ran 52 tests OK before implementation.
- Post-implementation targeted gate: same dependency suite ran 53 tests OK after adding regression coverage.
- Full gate: `python -m unittest discover tests -v` ran 195 tests OK.
- Added regression coverage for:
  - V4 `agent-trace.jsonl` rows validating against `schemas/agent-trace-event.schema.json`.
  - `enrich --output-dir <relative path>` producing manifest-loadable provider cache paths.
- Machine validation of generated sample bundles passed for 15 artifact rows:
  - manifest validates against `schemas/run-manifest.schema.json`
  - each artifact entry validates against `schemas/run-artifact.schema.json`
  - manifest paths are relative and loadable
  - `sha256`, `bytes`, and JSONL `record_count` match file contents
  - every non-null `schema_ref` validates the JSON document or each JSONL row
  - rejected HOMO fact is excluded from `scoring-view.json`, eligible LUMO remains, and recompute marker points to `canonical-evidence.json` and `scoring-view.json`

Implementation fixes from freeze validation:

- `src/spirosearch/v4_runtime.py`: decorates V4 trace rows with `event_id`, `run_id`, and `generated_at` before writing `agent-trace.jsonl`.
- `src/spirosearch/enrichment_runtime.py`: resolves `output_dir` internally so relative output directories still produce provider cache manifest paths that are loadable relative to the output directory.

## Current Known Dirty State

- Intended branch changes:
  - `src/spirosearch/enrichment_runtime.py`
  - `src/spirosearch/v4_runtime.py`
  - `tests/test_enrichment_runtime_cli.py`
  - `tests/test_v4_runtime_cli.py`
  - `plans/v11-loop-spec.md`
  - `plans/v11-loop-state.md`
- Generated local files removed before commit:
  - `.v11-samples/`
  - `uv.lock`
- Do not use `git add -A`.
- Keep generated outputs and agent configuration out of the commit.

## Frozen Artifact Matrix

| Bundle | Kind | Path | Format | Schema ref | Sample count | Join keys | Depends on | V11 use |
|---|---|---|---|---|---:|---|---|---|
| enrichment | `agent_trace` | `agent-trace.jsonl` | jsonl | `schemas/agent-trace-event.schema.json` | 1 | `event_id`, `candidate_id`, `review_item_id`, `lookup_id`, `cache_key`, `response_id` | none | Provider Lineage, error/timeline |
| enrichment | `canonical_evidence` | `canonical-evidence.json` | json | `schemas/canonical-evidence.schema.json` | null | `candidate_id`, `material_id`, `use_instance_id`, `energy_evidence_id`, `review_item_id` | `enrichment_results`, `review_events` | Candidate Flow, Review Worklist |
| enrichment | `enrichment_results` | `enrichment-results.json` | json | `schemas/enrichment-results.schema.json` | null | `candidate_id`, `review_item_id`, `lookup_id`, `cache_key`, `response_id` | `provider_cache_index` | Candidate Flow |
| enrichment | `provider_cache` | `provider-cache.jsonl` | jsonl | `schemas/provider-cache.schema.json` | 1 | `cache_key`, `response_id`, `lookup_id`, `provider` | none | Provider Lineage |
| enrichment | `provider_cache_index` | `provider-cache-index.json` | json | `schemas/provider-cache-index.schema.json` | null | `candidate_id`, `provider`, `lookup_id`, `cache_key`, `response_id`, `trace_event_id` | `provider_cache` | Provider Lineage |
| enrichment | `recompute_markers` | `recompute-markers.jsonl` | jsonl | `schemas/recompute-marker.schema.json` | 1 | `marker_id`, `review_event_id`, `review_item_id`, `candidate_id`, `target_id` | `review_events`, `canonical_evidence`, `scoring_view` | Review closure, recomputation |
| enrichment | `review_events` | `review-events.jsonl` | jsonl | `schemas/review-event.schema.json` | 1 | `review_item_id`, `event_id`, `target_id` | `review_queue` | Review Worklist |
| enrichment | `review_queue` | `review-queue.jsonl` | jsonl | `schemas/review-queue-item.schema.json` | 0 | `review_item_id`, `candidate_id`, `target_id`, `trace_event_id` | `enrichment_results`, `canonical_evidence`, `agent_trace` | Review Worklist |
| enrichment | `review_summary` | `review-summary.json` | json | `schemas/review-summary.schema.json` | null | `review_item_id`, `event_id`, `marker_id` | `review_queue`, `review_events`, `recompute_markers` | Review Worklist, Run Overview |
| enrichment | `scoring_view` | `scoring-view.json` | json | `schemas/scoring-view.schema.json` | null | `candidate_id`, `material_id`, `evidence_id` | `canonical_evidence`, `review_queue` | Scoring Eligibility |
| v4-round | `agent_trace` | `agent-trace.jsonl` | jsonl | `schemas/agent-trace-event.schema.json` | 1 | `event_id`, `candidate_id`, `review_item_id`, `lookup_id`, `cache_key`, `response_id` | none | Timeline, diagnostics |
| v4-round | `ledger` | `ledger.jsonl` | jsonl | null | 2 | `candidate_id`, `request_id` | none | Experiment flow |
| v4-round | `model_updates` | `model-updates.jsonl` | jsonl | null | 0 | `candidate_id`, `request_id`, `experiment_id` | `ledger`, `posterior` | Active-learning diagnostics |
| v4-round | `posterior` | `posterior.json` | json | null | null | `candidate_id` | `ledger` | Active-learning state |
| v4-round | `recommendations` | `recommendations.json` | json | null | null | `candidate_id`, `request_id` | `ledger`, `posterior` | Recommendation queue |

## Frozen Consumer Rules

- `run-manifest.json` is the only artifact discovery source; readers must not guess default filenames.
- Manifest paths must be safe relative paths. Repository/API/frontend should resolve paths relative to the manifest output directory.
- `schema_ref` uses the singular schema filenames currently emitted by manifest metadata, for example `schemas/review-event.schema.json` and `schemas/recompute-marker.schema.json`.
- Non-null `schema_ref` means payload validation is required; `schema_ref = null` means legacy or runtime-specific artifact payload, not schema absence in the manifest contract.
- JSON artifact `record_count` is always `null`; UI counts must read payload fields or arrays. JSONL `record_count` is the count of non-empty lines.
- `scoring-view.json` is a policy-filtered read model. It excludes rejected facts, facts blocked by review, facts missing `reference_scale`, and provider confidence.
- `scoring-view.json` energy facts do not carry `candidate_id`; consumers that need candidate joins must use manifest join keys plus `canonical-evidence.json`.
- `review-events.jsonl` order is semantic; closure uses input order, so API/repository layers must not reorder events before applying closure.
- `recompute-markers.jsonl` `affected_artifacts` values are filenames, not artifact kinds or manifest paths.
- Missing optional conflict/performance artifacts should produce local `unavailable` state for that panel only, not fail the whole run view.

## Next Slice

```text
Slice: v11-dependency-freeze
Goal: verify the V10 artifact baseline V11 will consume.
Stop condition:
  - manifest artifact entries include path/schema_ref/sha256/bytes/record_count/join_keys
  - scoring-view.json validates from manifest discovery
  - provider confidence is absent from scoring-effect inputs
  - unresolved blocking review and missing reference_scale facts are excluded from scoring view
  - review summary and recompute markers exist and agree on affected candidate/evidence IDs
  - blockers are written here before any V11 implementation slice starts
```

## Suggested Verification

Use targeted verification from the V11 dependency-freeze worktree.

```powershell
$env:PYTHONPATH='src'; uv run python -m unittest tests.test_scoring_view tests.test_run_artifacts tests.test_artifact_viewer tests.test_review_runtime -v
```

Before merge of any V11 implementation slice:

```powershell
$env:PYTHONPATH='src'; uv run python -m unittest discover tests -v
Test-Path uv.lock
git status --short --branch
```

## Loop Queue

1. `v11-dependency-freeze`
   - Status: ready to start.
   - Output: dependency matrix with verified artifact names, schema refs, hashes, and join keys.

2. `v11-repository-facade-json-backend`
   - Status: pending dependency freeze.
   - Output: repository boundary plan or implementation slice using existing JSON/JSONL artifacts.

3. `v11-artifact-validation-local-loop`
   - Status: pending dependency freeze.
   - Output: local artifact validation loop covering manifest, schema, hash, JSONL, and join keys.

4. `v11-readonly-api-mcp-inventory`
   - Status: pending repository facade.
   - Output: read-only manifest, artifact, scoring view, review summary, and provider lineage surface inventory.

5. `v11-visualization-readiness-fixtures`
   - Status: pending dependency freeze.
   - Output: frontend fixture matrix for diagnostic panels.

## Frontend Readiness Matrix

| Panel | Required artifacts | V11 blocker |
|---|---|---|
| Run Overview | `run-manifest.json` | Needs dependency-freeze sample manifest |
| Candidate Flow | candidate pool, enrichment results, canonical evidence, scoring view | Needs dependency-freeze join-key matrix |
| Scoring Eligibility | `scoring-view.json`, review queue/summary | Needs dependency-freeze sample artifact bundle |
| Review Worklist | review queue, review events, review summary | Needs dependency-freeze review closure sample |
| Provider Lineage | provider cache index, agent trace, provenance/raw hash | Provider lineage shape not frozen |
| Conflict Panel | conflict events, canonical evidence | Conflict artifacts optional; UI must degrade |
| Performance/Error Timeline | benchmark notes, trace/error artifacts | Benchmark artifact shape not selected |

## Open Decisions

- Which second repository backend to evaluate first after JSON/JSONL: SQLite or Parquet/Arrow.
- Whether P1 API and MCP should share one response envelope for structured `unavailable`.
- Which benchmark thresholds are meaningful enough before Polars/Arrow replaces any hot path.
- Which visualization panel should be implemented first after V10 closure: Scoring Eligibility or Review Worklist.

## Loop Caps

- `MAX_PARALLEL=2`
- One implementation slice per branch.
- One reviewer pass before merge.
- Stop after two failed full-test attempts and write a blocker note here.
- No Prefect Server dependency in P0; local loops must remain runnable without scheduling infrastructure.
