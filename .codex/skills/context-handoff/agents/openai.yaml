interface:
  display_name: "Context Handoff"
  short_description: "Save, restore, and hand off working context"
  default_prompt: "Use $context-handoff to save or restore the current working context."

