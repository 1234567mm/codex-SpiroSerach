// Workbench view ids — the main navigation surface of the AtomReasonX shell.

export type WorkbenchViewId =
  | "session"
  | "search"
  | "database"
  | "knowledge"
  | "screening"
  | "workflow"
  | "projects";
